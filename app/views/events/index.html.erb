<br>
<br>
<br>
  <% @events.each do |event| %>

      <h3><%= event.title %></h3>

      <p><%= event.description %></p>


      <%= link_to "Read More", event_path(event), :class => 'btn btn-primary' %>
      <hr>
  <% end %>
